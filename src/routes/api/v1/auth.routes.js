/**
 * @file auth.routes.js
 * @description Authentication Route Definitions for /api/v1/auth.
 * Maps endpoints (POST /register, POST /login, POST /logout, POST /refresh) to validation middlewares and auth controller.
 */
