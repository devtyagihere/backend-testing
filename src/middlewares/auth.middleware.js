/**
 * @file auth.middleware.js
 * @description Authentication & Authorization Guard Middleware.
 * Validates JWT access tokens from request headers/cookies and attaches authenticated user to request context.
 * Provides role-based access control (RBAC) authorization checks.
 */
